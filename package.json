{
  "name": "taskflow",
  "version": "1.0.0",
  "description": "Full-stack Jira/Trello-like project management platform (MERN stack)",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"backend,frontend\" --prefix-colors \"cyan,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "concurrently --names \"backend,frontend\" --prefix-colors \"cyan,magenta\" \"npm run start --prefix backend\" \"npm run start --prefix frontend\"",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "test": "concurrently --names \"backend,frontend\" --prefix-colors \"cyan,magenta\" \"npm test --prefix backend\" \"npm test --prefix frontend\"",
    "test:backend": "npm test --prefix backend",
    "test:frontend": "npm test --prefix frontend",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
